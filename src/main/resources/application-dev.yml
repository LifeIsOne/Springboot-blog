server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 5000

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2mem:test;MODE=MySQL
    username: sa
    password:

#      콘솔 url 설정. 웹으로 접근할 수 있게
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create